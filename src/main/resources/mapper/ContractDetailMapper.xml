<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.mapper.ContractDetailMapper">
    <!-- 插入合同详情 -->
    <insert id="insertContractDetail">
        INSERT INTO contract_detail (contract_id, product_id, quantity, price)
        VALUES (#{contractId}, #{productId}, #{quantity}, #{price})
    </insert>
</mapper>

