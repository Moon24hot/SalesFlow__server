<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.mapper.ContractMapper">

    <!-- 插入合同信息 -->
    <insert id="insertContract" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO contract (customer_id, salesperson_id, amount, sign_date)
        VALUES (#{customerId}, #{salespersonId}, #{amount}, #{signDate})
    </insert>

</mapper>